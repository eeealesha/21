# Checklist: Проект 3 — Python + Pandas

## Введение

Проверка — это не формальность. Это ключевая часть обучения.

Когда ты проверяешь чужое решение, ты видишь другой подход к той же задаче. Может быть, более элегантный. Может быть, с ошибкой, которую ты сам чуть не совершил. Может быть, с идеей, которая изменит твоё понимание Pandas.

Не ищи ошибки ради ошибок. Ищи понимание. Задавай вопросы. Обсуждай решения. Именно в этом разговоре — настоящее обучение.

---

## Общие правила проверки

- На проверку принимаются только файлы, которые находятся в папке `src/` ветки `develop` репозитория проверяемого участника.
- Убедись, что ты открыл репозиторий именно того человека, которого должен проверить.
- Перечитай задание перед проверкой каждого пункта.
- **Перед запуском каждого ноутбука выполни: Kernel → Restart & Run All.** Ноутбук должен выполниться без ошибок от первой до последней ячейки. Если возникает ошибка — это считается незачётом соответствующего пункта.
- **EMPTY WORK** — поставь этот флаг, если репозиторий пустой, ветка `develop` отсутствует, или файлы задания находятся не в папке `src/` ветки `develop`. Продолжи проверку, чтобы помочь разобраться.
- **CHEAT** — поставь этот флаг, если участник не может объяснить логику своего кода, не понимает, что делает каждая строка, или решения явно идентичны чужим без понимания. Продолжи проверку.

---

## Задание 1: Первый скальпель

**Цель:** освоить интерфейс Jupyter Notebook и базовые конструкции Python.

- [ ] В папке `src/` присутствует файл `task1.ipynb`.
- [ ] Ноутбук запускается без ошибок (Kernel → Restart & Run All).
- [ ] Присутствует Markdown-ячейка с именем автора (заголовок первого уровня) и описанием проекта.
- [ ] Созданы переменные `clinic_name` (тип `str`), `branches_count` (тип `int`), `specialties` (тип `list`, содержит 5 элементов).
- [ ] Цикл `for` проходит по `specialties` и выводит каждую специализацию с f-строкой.
- [ ] Словарь `doctors` содержит 3 ключа, каждое значение — вложенный словарь с полями `specialty` и `appointments`.
- [ ] Функция `calculate_avg_revenue` принимает список чисел, возвращает `float`, протестирована на конкретном примере.
- [ ] Функция `classify_wait` корректно возвращает `'норма'` / `'высокое'` / `'критично'`. Проверь граничные случаи: `classify_wait(44)` → `'норма'`, `classify_wait(45)` → `'высокое'`, `classify_wait(60)` → `'высокое'`, `classify_wait(61)` → `'критично'`.

**Вопрос для проверки понимания:**

> «Чем список отличается от словаря в Python? Когда ты выбрал бы список, а когда словарь?»

---

## Задание 2: Анатомия датафрейма

**Цель:** научиться загружать данные и проводить первичный исследовательский анализ (EDA).

- [ ] В папке `src/` присутствует файл `task2.ipynb`.
- [ ] Ноутбук запускается без ошибок.
- [ ] `clinic_visits.csv` загружен через `pd.read_csv()`, выведены `.head(10)` и `.info()`.
- [ ] Присутствует Markdown-ячейка с комментарием по `.info()`: количество строк, столбцов, наличие пропусков.
- [ ] Выведены `.describe()` и `.nunique()`. Есть Markdown-комментарий о типах столбцов.
- [ ] Создан `df_subset` ровно с 5 столбцами: `visit_date`, `specialty`, `status`, `revenue`, `wait_time_min`.
- [ ] Создан `df_conducted` — фильтрация по `status == 'Проведён'`. Выведено количество строк.
- [ ] Пропуски найдены через `.isna().sum()`. Столбец `age` заполнен медианой (не константой — проверь, что используется `.median()`). Столбец `district` заполнен строкой `'Не указан'`. После заполнения пропусков нет.
- [ ] `.value_counts()` по `specialty` выведены в порядке убывания.
- [ ] Найдены 5 записей с наибольшей `revenue` (`.nlargest()` или `.sort_values().tail(5)`).

**Вопрос для проверки понимания:**

> «Что делает `.info()` и что делает `.describe()`? Какую информацию даёт первый метод, но не даёт второй?»

---

## Задание 3: Лаборатория трансформаций

**Цель:** научиться группировать данные, создавать вычисляемые признаки и применять функции.

- [ ] В папке `src/` присутствует файл `task3.ipynb`.
- [ ] Ноутбук запускается без ошибок.
- [ ] `.groupby('specialty').agg(...)` содержит как минимум три агрегата (sum, mean, count). Результат отсортирован по убыванию суммарной выручки.
- [ ] Доля отменённых рассчитана через groupby + условие (например, `(x == 'Отменён').sum() / len(x)` или аналог). Не просто `.value_counts()` по всей таблице. Результат в порядке убывания.
- [ ] Столбец `revenue_category` добавлен через `pd.cut()` с тремя метками. Разбивка равноинтервальная (не квантильная). Выведено количество в каждой категории.
- [ ] Столбец `age_group` добавлен через `.apply()`. Проверь граничные значения: 18 → `'18–40'`, 40 → `'18–40'`, 41 → `'41–60'`, 60 → `'41–60'`, 61 → `'60+'`.
- [ ] Топ-5 врачей выведены через groupby + sort_values + head(5).

**Вопрос для проверки понимания:**

> «Чем `.apply()` отличается от векторизованной операции (например, `df['col'] * 2`)? Приведи пример задачи, где `.apply()` — единственный вариант.»

---

## Задание 4: Операция слияния

**Цель:** научиться объединять датафреймы, работать с датами и извлекать данные из PDF.

- [ ] В папке `src/` присутствуют файлы `task4.ipynb` и `task4_report.xlsx`.
- [ ] Ноутбук запускается без ошибок.
- [ ] Файлы соединены через `pd.merge(how='left')` по `doctor_id`. Есть Markdown-объяснение выбора `left`.
- [ ] Столбец `visit_date` преобразован через `pd.to_datetime()`. Тип — `datetime64`.
- [ ] Созданы столбцы `visit_month` и `visit_year` через аксессор `.dt`.
- [ ] Группировка по месяцам выполнена. Самый загруженный месяц назван в Markdown.
- [ ] Стаж `tenure_years` рассчитан через арифметику дат (не хардкоженный год!). Видна формула с `pd.to_datetime('today')`.
- [ ] PDF открыт через `pdfplumber`. Выведены «сырые» данные из `.extract_table()`.
- [ ] Из «сырых» данных создан датафрейм: убраны пробелы (`.str.strip()`), `'—'` заменено на `0`, числовые столбцы приведены к числовому типу, столбцы переименованы корректно.
- [ ] Очищенный датафрейм сохранён в `task4_report.xlsx` без индекса.

**Вопрос для проверки понимания:**

> «Что произойдёт с результатом merge, если в правой таблице нет `doctor_id`, который есть в левой, при `how='left'` — а теперь при `how='inner'`? Покажи разницу на примере.»

---

## Задание 5: Визуальный диагност

**Цель:** научиться строить информативные и корректно оформленные графики.

- [ ] В папке `src/` присутствуют: `task5.ipynb`, `chart1_wait_time.png`, `chart2_revenue_by_specialty.png`, `chart3_scatter.png`, `chart4_boxplot.png`.
- [ ] Ноутбук запускается без ошибок.
- [ ] Все четыре графика строятся только на данных со статусом `'Проведён'`.
- [ ] Гистограмма `wait_time_min`: построена через matplotlib, указан параметр `bins`, есть русский заголовок и подписи обеих осей.
- [ ] Горизонтальный barplot по суммарной выручке: специализации отсортированы по убыванию суммы. Есть русские подписи.
- [ ] Scatter plot `wait_time_min` vs `revenue`: цвет точек по `specialty` (параметр `hue`), присутствует легенда, русские подписи.
- [ ] Boxplot `revenue` по специализациям: подписи не перекрываются, русский заголовок.
- [ ] Все 4 PNG-файла сохранены через `plt.savefig()`, а не сделаны скриншотами (проверь, что код `.savefig()` присутствует в ячейках).
- [ ] Нет визуального мусора: нет 3D-эффектов, шрифт читаем, подписи не обрезаны.

**Вопрос для проверки понимания:**

> «Почему у гистограммы есть параметр `bins`? Что изменится в интерпретации графика, если поставить `bins=3` вместо `bins=20`?»

---

## Задание 6: Статистический детектив

**Цель:** научиться применять статистические тесты и содержательно интерпретировать их результаты.

- [ ] В папке `src/` присутствует файл `task6.ipynb`.
- [ ] Ноутбук запускается без ошибок.
- [ ] Все расчёты выполнены только на данных со статусом `'Проведён'`.
- [ ] Описательные статистики для `revenue` и `wait_time_min` включают: mean, median, mode, std и три перцентиля (Q25, Q50, Q75).
- [ ] Тест Шапиро-Уилка применён отдельно к выборкам хирургов и терапевтов. Выведены p-value обеих групп. Есть Markdown-вывод о нормальности.
- [ ] Выбор теста (t-test vs Mann-Whitney) обоснован результатом Shapiro. Код содержит условную логику (`if`/`else`) или явное объяснение выбора — не просто захардкоженный один тест.
- [ ] Статистика и p-value теста выведены. Есть Markdown-вывод: отвергается ли H₀ и что это означает содержательно для клиники.
- [ ] Таблица сопряжённости для хи-квадрат построена (через `pd.crosstab()` или `groupby`). Передана в `stats.chi2_contingency()`. Выведены χ², p-value, степени свободы. Есть Markdown-вывод.
- [ ] Итоговая Markdown-ячейка «Выводы и рекомендации» присутствует и содержит содержательный текст (не просто пересказ числа p-value, а что это означает для клиники).

**Вопрос для проверки понимания:**

> «Что такое p-value? Если p-value = 0.03 при уровне значимости α = 0.05 — отвергаем ли мы нулевую гипотезу? Что конкретно это означает для клиники — в словах, без формул?»

---

*Ты завершил проверку. Если нашёл интересный подход — скажи об этом. Если видишь ошибку — объясни, почему это ошибка, а не просто отметь. Помни: цель проверки — не оценить, а помочь.*
