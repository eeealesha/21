# Checklist: Проект 6 — Data-Driven подход

## Введение

Это финальный проект. Это значит, что здесь нет одной правильной функции, которую нужно написать, или одного верного числа, которое нужно вычислить.

Когда ты проверяешь этот проект, смотри на логику в целом: насколько связно студент прошёл путь от бизнес-вопроса до рекомендации? Понимает ли он, почему выбрал именно эту модель и эти метрики? Умеет ли объяснить результат директору, а не только другому аналитику?

Задавай вопросы. Проси объяснить. Обсуждай альтернативы. Именно здесь происходит настоящее обучение.

---

## Общие правила проверки

- На проверку принимаются только файлы, которые находятся в папке `src/` ветки `develop` репозитория проверяемого участника.
- Убедись, что ты открыл репозиторий именно того человека, которого должен проверить.
- Перечитай задание перед проверкой каждого пункта.
- **Перед запуском каждого ноутбука выполни: Kernel → Restart & Run All.** Ноутбук должен выполниться без ошибок от первой до последней ячейки. Если возникает ошибка — это считается незачётом соответствующего пункта.
- **EMPTY WORK** — поставь этот флаг, если репозиторий пустой, ветка `develop` отсутствует, или файлы задания находятся не в папке `src/`. Продолжи проверку, чтобы помочь разобраться.
- **CHEAT** — поставь этот флаг, если участник не может объяснить логику своего анализа или решения явно идентичны чужим без понимания. Продолжи проверку.

---

## Задание 1: CRISP-DM и постановка задачи

**Цель:** применить методологию CRISP-DM и формализовать бизнес-задачу.

- [ ] В папке `src/` присутствует файл `task1_crisp_dm.md`.
- [ ] Таблица 6 фаз CRISP-DM заполнена применительно к задаче удержания пациентов (не просто названия фаз — конкретные действия в этом проекте).
- [ ] Сформулирована целевая метрика (Retention Rate) с формулой расчёта.
- [ ] Написаны H₀ и H₁ в формальном виде.
- [ ] Указан критерий успеха (что считается хорошим результатом).
- [ ] Описана инфраструктура data-driven подхода (минимум 3 компонента + роли команды).

**Вопрос для проверки понимания:**

> «Назови 6 фаз CRISP-DM. На какой фазе ты сейчас находишься в этом проекте — и почему? Какая фаза, по твоему опыту из этого проекта, заняла больше всего времени?»

---

## Задание 2: Дерево метрик

**Цель:** построить иерархию KPI клиники и выбрать North Star метрику.

- [ ] В папке `src/` присутствует файл `task2.ipynb`.
- [ ] В папке `src/` присутствует файл `task2_kpi_tree.md`.
- [ ] Ноутбук запускается без ошибок.
- [ ] KPI-дерево содержит три уровня (стратегический / тактический / операционный).
- [ ] Для каждой метрики в дереве: формула расчёта, источник данных, целевое значение.
- [ ] В ноутбуке рассчитаны метрики тактического и операционного уровней (не вручную — через pandas).
- [ ] Выбрана и обоснована North Star метрика.
- [ ] Построен heatmap корреляций метрик по врачам (`doctor_id`). Есть Markdown-вывод.

**Вопрос для проверки понимания:**

> «Что такое North Star метрика? Почему для бизнеса важно иметь одну такую метрику, а не несколько? Покажи в своём дереве, как операционные метрики влияют на стратегическую.»

---

## Задание 3: Подготовка данных

**Цель:** пройти ETL-цикл и создать аналитическую таблицу на уровне пациента.

- [ ] В папке `src/` присутствует файл `task3.ipynb`.
- [ ] Ноутбук запускается без ошибок.
- [ ] `patients.csv` и `visits.csv` объединены через `pd.merge`.
- [ ] Рассчитаны все 8 признаков на уровне пациента: `total_visits`, `conducted_visits`, `cancel_rate`, `avg_revenue`, `avg_wait_time`, `days_since_last_visit`, `churned`, `tenure_months`.
- [ ] `churned` рассчитан как `days_since_last_visit > 180` (не хардкожено вручную — через дату 2024-12-31).
- [ ] Проведён EDA: гистограммы числовых признаков, countplot churned, heatmap корреляций.
- [ ] Markdown-вывод: топ-3 признака, коррелирующие с `churned`.

**Вопрос для проверки понимания:**

> «Зачем агрегировать данные на уровень пациента, если можно анализировать каждый визит отдельно? Какую информацию мы теряем при агрегации?»

---

## Задание 4: Анализ факторов оттока

**Цель:** найти ключевые драйверы оттока и построить предсказательную модель.

- [ ] В папке `src/` присутствует файл `task4.ipynb`.
- [ ] Ноутбук запускается без ошибок.
- [ ] Когортный анализ выполнен: пациенты сгруппированы по кварталу первого визита, рассчитан Retention Rate для трёх горизонтов (90/180/365 дней).
- [ ] Построен lineplot динамики retention по когортам.
- [ ] Для каждого из 4 факторов (возраст, страхование, ожидание, cancel rate) — boxplot/barplot с разбивкой churned/retained + Markdown-вывод.
- [ ] Логистическая регрессия обучена: признаки включают демографию + поведенческие признаки + dummy для `insurance_type`.
- [ ] Вывести: accuracy, ROC AUC, classification report.
- [ ] Построена ROC-кривая с AUC на графике.
- [ ] Выведены топ-3 коэффициента с Markdown-интерпретацией.

**Вопрос для проверки понимания:**

> «Что показывает ROC AUC = 0.9? Чем AUC лучше, чем просто accuracy для задачи предсказания оттока? Что означает точка (0.0, 0.0) и (1.0, 1.0) на ROC-кривой?»

---

## Задание 5: Стратегия удержания пациентов

**Цель:** сегментировать пациентов по риску и сформулировать конкретные рекомендации.

- [ ] В папке `src/` присутствует файл `task5.ipynb`.
- [ ] В папке `src/` присутствует файл `task5_strategy.md`.
- [ ] Ноутбук запускается без ошибок.
- [ ] Сегментация выполнена через `predict_proba`: три сегмента (низкий/средний/высокий риск). Порог 0.30 и 0.60 виден в коде.
- [ ] Выведены размер каждого сегмента и средние значения признаков по сегментам.
- [ ] Написаны Markdown-рекомендации для каждого сегмента (конкретные, а не «улучшить сервис»).
- [ ] `task5_strategy.md` содержит таблицу ICE-оценки с минимум 5 инициативами.
- [ ] ICE = Impact × Confidence × Ease (не сумма!). Таблица отсортирована по убыванию.
- [ ] Выполнен ROI-расчёт: числа обоснованы из данных (не взяты из головы).

**Вопрос для проверки понимания:**

> «Почему ICE = Impact × Confidence × Ease, а не (I + C + E) / 3? Приведи пример инициативы, у которой Impact = 10, но ICE окажется низким — и объясни, почему это правильно.»

---

## Задание 6: Автоматизация и финальный отчёт

**Цель:** описать систему мониторинга и написать полный CRISP-DM отчёт.

- [ ] В папке `src/` присутствует файл `task6_summary.ipynb`.
- [ ] В папке `src/` присутствует файл `task6_report.md`.
- [ ] Ноутбук запускается без ошибок.
- [ ] `task6_summary.ipynb` содержит сводную таблицу всех метрик, результатов модели и ROI в одном месте.
- [ ] Итоговая Markdown-ячейка «Выводы и рекомендации» содержит связный текст (не список из 3 bullet points).
- [ ] `task6_report.md` содержит все 6 разделов CRISP-DM (по одному на каждую фазу).
- [ ] Раздел Deployment включает конкретный план: кто, что, когда, как проверить результат.
- [ ] Описана система мониторинга: таблица метрик с частотой и порогами + схема pipeline.
- [ ] Присутствует псевдокод функции `check_retention_alert` (или аналогичной).
- [ ] Присутствует «Резюме для директора» — не более одной страницы, без формул и технических терминов.

**Вопрос для проверки понимания:**

> «Зачем нужен автоматический триггер мониторинга, если аналитик может проверять метрики вручную раз в неделю? Что может пойти не так за ту неделю, пока аналитик не смотрит?»

---

*Ты завершил проверку. Это был финальный проект серии — значит, у тебя и у проверяемого за плечами шесть проектов: Excel, SQL, Pandas, дашборды, A/B тест и полный data-driven цикл. Это серьёзно. Обсуди с пиром, что далось сложнее всего — и что оказалось неожиданно понятным. Это тоже часть обучения.*
