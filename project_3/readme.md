# Project 3: Живые данные

**Аннотация:** Excel остался позади. SQL открыл двери в базы данных. Теперь — Python. Язык, на котором говорит современная аналитика данных. Pandas, визуализация, статистика — инструменты, которыми пользуются аналитики в реальных компаниях. Клиника «Здоровье+» снова ждёт тебя — на этот раз с ноутбуком и кодом.

---

## Содержание

- [Chapter I — Рекомендации к проекту](#chapter-i--рекомендации-к-проекту)
- [Chapter II — Задания](#chapter-ii--задания)
  - [Задание 1: Первый скальпель](#задание-1-первый-скальпель)
  - [Задание 2: Анатомия датафрейма](#задание-2-анатомия-датафрейма)
  - [Задание 3: Лаборатория трансформаций](#задание-3-лаборатория-трансформаций)
  - [Задание 4: Операция слияния](#задание-4-операция-слияния)
  - [Задание 5: Визуальный диагност](#задание-5-визуальный-диагност)
  - [Задание 6: Статистический детектив](#задание-6-статистический-детектив)

---

## Chapter I — Рекомендации к проекту

Этот документ — твоя главная точка опоры на протяжении всего проекта. Никаких лекций, никаких преподавателей — только задания, коллеги и открытые источники.

**Как работать с проектом:**

- Клонируй репозиторий с GitLab.
- Создай ветку `develop` — вся твоя работа ведётся только в ней.
- Помещай все файлы с решениями в папку `src/` внутри ветки `develop`.
- На проверку принимаются только файлы, которые находятся в папке `src/` ветки `develop`.
- Загружай только те файлы, которые указаны в задании.

**Установка окружения:**

Перед началом работы установи все необходимые библиотеки:

```
pip install jupyter pandas matplotlib seaborn pdfplumber fpdf2 scipy openpyxl
```

**Создание датасетов:**

Перед выполнением заданий сгенерируй файлы данных. Запусти скрипт из корневой папки проекта:

```
python datasets/create_datasets.py
```

Скрипт создаст три файла в папке `datasets/`:
- `clinic_visits.csv` — основной датасет визитов (500 строк)
- `clinic_staff.csv` — данные о персонале клиники (30 строк)
- `quarterly_report.pdf` — квартальный отчёт в формате PDF

Эти файлы не нужно коммитить в репозиторий.

**Как запустить Jupyter Notebook:**

```
jupyter notebook
```

Создавай каждое задание как отдельный файл `.ipynb` с именем `task1.ipynb`, `task2.ipynb`, ..., `task6.ipynb`.

**Используй все доступные ресурсы:**

Гугли, читай документацию pandas и scipy, задавай вопросы ИИ-ассистентам — это не запрещено, это часть процесса. Но будь критичен к источникам: проверяй, анализируй, сравнивай. То, что выдаёт ИИ или первая ссылка в поиске, не всегда верно.

**Проси помощи — и сам помогай:**

Если застрял — обращайся к коллегам. Если видишь, что кто-то застрял, предложи помощь. Обмен идеями — это не читинг, это суть peer-to-peer обучения.

**Не копируй — понимай:**

Скопировать код легко. Но на защите тебя спросят: почему ты использовал `.apply()`, а не векторизованную операцию? Чем `merge` с `how='left'` отличается от `how='inner'`? Что такое p-value и почему твой результат именно такой? Понимание важнее результата. Если ты понял, как работает groupby — ты можешь объяснить это другому. Если просто скопировал — нет.

**Формат сдачи:**

- Задания 1, 2, 3, 5, 6: по одному файлу `.ipynb` (`task1.ipynb` ... `task6.ipynb`)
- Задание 4: `task4.ipynb` + файл `task4_report.xlsx`
- Задание 5: `task5.ipynb` + четыре файла `.png`

Все файлы кладутся в папку `src/` ветки `develop`.

**Помни: процесс важнее результата.**

---

## Chapter II — Задания

### Контекст

Ты — аналитик данных в клинике «Здоровье+». Коллеги из IT-отдела передали тебе три файла: CSV с данными о визитах, CSV со списком персонала, и PDF с квартальным отчётом. Excel для этих объёмов уже неудобен, SQL хорош для запросов, но для полноценного анализа нужен Python. Пришло время освоить инструментарий настоящего аналитика.

---

### Задание 1: Первый скальпель

**Для выполнения нужно знать:** интерфейс Jupyter Notebook, Markdown-ячейки, переменные и типы данных Python (`str`, `int`, `float`, `bool`), списки (`list`), словари (`dict`), цикл `for`, функции (`def`), f-строки, команда `pip install`.

Аналитик без инструмента — как хирург без скальпеля. Перед тем как резать данные, нужно подготовить операционную: настроить окружение, освоить Jupyter Notebook и написать первый Python-код. Клиника «Здоровье+» ждёт аналитика. Начни с азов.

**Выполни следующие шаги:**

1. Убедись, что все библиотеки установлены (команда из раздела «Установка окружения»). Запусти Jupyter Notebook и создай файл `task1.ipynb`.

2. Создай Markdown-ячейку в начале ноутбука с заголовком первого уровня (`# Имя Фамилия — Проект 3`) и кратким описанием проекта в 2–3 предложениях.

3. Создай переменные:
   - `clinic_name` — строка с названием клиники
   - `branches_count` — целое число (количество филиалов)
   - `specialties` — список из 5 специализаций клиники

4. С помощью цикла `for` пройди по списку `specialties` и выведи каждую специализацию в формате:
   `f"Специализация: {specialty}"`

5. Создай словарь `doctors` с тремя записями. Ключ — имя врача (строка), значение — вложенный словарь с полями `specialty` (строка) и `appointments` (int). Выведи словарь с помощью `print()`.

6. Напиши функцию `calculate_avg_revenue(revenue_list)`, которая принимает список числовых значений и возвращает их среднее значение как `float`. Протестируй функцию на списке из 5 произвольных чисел.

7. Напиши функцию `classify_wait(minutes: int) -> str`, которая возвращает:
   - `'норма'` — если ожидание менее 45 минут
   - `'высокое'` — если от 45 до 60 минут включительно
   - `'критично'` — если более 60 минут

   Протестируй функцию на значениях: 30, 45, 60, 61.

**Результат:** файл `task1.ipynb` в папке `src/`.

---

### Задание 2: Анатомия датафрейма

**Для выполнения нужно знать:** `import pandas as pd`, `pd.read_csv()`, методы `.head()`, `.tail()`, `.info()`, `.describe()`, атрибуты `.shape`, `.columns`, `.dtypes`, выбор столбцов (`df[['col1', 'col2']]`), булева фильтрация, `.isna()`, `.fillna()`, `.dropna()`, `.value_counts()`.

Пациент поступил. Первым делом — анамнез: откуда он, сколько лет, что болит. Работа с новым датасетом начинается так же: загрузи, осмотри, зафиксируй. Данные клиники «Здоровье+» у тебя на столе — изучи их анатомию.

**Выполни следующие шаги:**

1. Загрузи файл `datasets/clinic_visits.csv` с помощью `pd.read_csv()`. Выведи первые 10 строк с помощью `.head(10)`, затем вызови `.info()`. Добавь Markdown-ячейку с комментарием: сколько строк и столбцов в датафрейме, есть ли пропуски и в каких столбцах.

2. Выведи описательную статистику с помощью `.describe()`. Для каждого столбца посчитай количество уникальных значений (`.nunique()`). Добавь Markdown-ячейку: какие столбцы категориальные, какие числовые?

3. Создай переменную `df_subset` с пятью столбцами: `visit_date`, `specialty`, `status`, `revenue`, `wait_time_min`.

4. Отфильтруй из исходного датафрейма только записи со статусом `'Проведён'`. Сохрани в переменную `df_conducted`. Выведи количество таких записей.

5. Найди столбцы с пропущенными значениями (`.isna().sum()`). Заполни пропуски:
   - В столбце `age` — медианным значением (`.median()`). Округли до целого.
   - В столбце `district` — строкой `'Не указан'`

   Убедись, что пропуски устранены: выведи `.isna().sum()` после заполнения.

6. Подсчитай количество визитов по каждой специализации с помощью `.value_counts()`. Выведи результат в порядке убывания.

7. Найди 5 визитов с наибольшей выручкой (`revenue`). Выведи соответствующие строки.

**Результат:** файл `task2.ipynb` в папке `src/`.

---

### Задание 3: Лаборатория трансформаций

**Для выполнения нужно знать:** `.groupby()`, `.agg()`, именованные агрегаты, множественные агрегации, `.sort_values()`, `.apply()`, лямбда-функции, `pd.cut()`, `pd.qcut()`, `.map()`, добавление вычисляемых столбцов (`df['new_col'] = ...`).

Сырые данные — это руда. Аналитик — это аффинажный завод. В лаборатории трансформаций ты научишься плавить данные: группировать, агрегировать, добавлять новые признаки. Из руды — металл. Из строк — смысл.

**Выполни следующие шаги:**

1. Используя `.groupby()` и `.agg()`, рассчитай для каждой специализации:
   - суммарную выручку (`sum`)
   - среднюю выручку за визит (`mean`)
   - количество визитов (`count`)

   Отсортируй результат по суммарной выручке по убыванию.

2. Найди специализации с наибольшей долей отменённых визитов. Используй `.groupby()` по специализации — считай долю записей со статусом `'Отменён'` от общего числа в каждой группе. Выведи результат в порядке убывания доли.

3. Добавь в датафрейм новый столбец `revenue_category`. Используй `pd.cut()` для разбивки столбца `revenue` на 3 равных интервала с метками: `'низкая'`, `'средняя'`, `'высокая'`. Выведи количество записей в каждой категории.

4. Добавь столбец `age_group` используя `.apply()` с лямбда-функцией или именованной функцией. Правила:
   - `'до 18'` — возраст строго меньше 18
   - `'18–40'` — от 18 до 40 включительно
   - `'41–60'` — от 41 до 60 включительно
   - `'60+'` — старше 60

   > Перед добавлением столбца убедись, что в `age` нет пропусков (используй заполненный датафрейм из Задания 2 или заполни пропуски здесь снова).

   Выведи количество записей в каждой группе.

5. Найди топ-5 врачей по суммарной выручке с помощью `.groupby('doctor_name')['revenue'].sum()` и `.sort_values()`. Выведи таблицу с именами врачей и их суммарной выручкой.

**Результат:** файл `task3.ipynb` в папке `src/`.

---

### Задание 4: Операция слияния

**Для выполнения нужно знать:** `pd.merge()`, параметр `how` (`'left'`, `'inner'`, `'outer'`), ключи соединения (`on=`), `pd.to_datetime()`, аксессор `.dt` (`.dt.month`, `.dt.year`, `.dt.dayofweek`), арифметика с датами (`pd.Timestamp.today()`), библиотека `pdfplumber`, метод `.extract_table()`, `.to_excel()`.

Данные о визитах живут в одном файле, данные о персонале — в другом. PDF с квартальным отчётом — третий источник. Настоящая аналитика начинается там, где таблицы соединяются. Добро пожаловать в операционную слияний.

**Выполни следующие шаги:**

1. Загрузи файл `datasets/clinic_staff.csv`. Соедини его с `datasets/clinic_visits.csv` по столбцу `doctor_id` с помощью `pd.merge()` и `how='left'`. Выведи форму (`.shape`) итогового датафрейма. Добавь Markdown-ячейку: почему выбран именно `left` join, а не `inner`?

2. Преобразуй столбец `visit_date` в тип `datetime` с помощью `pd.to_datetime()`. Убедись, что тип изменился: выведи `.dtypes`.

3. Используя аксессор `.dt`, извлеки:
   - месяц из `visit_date` → новый столбец `visit_month`
   - год из `visit_date` → новый столбец `visit_year`

4. Подсчитай количество визитов по каждому месяцу (группируй по `visit_year` и `visit_month`). Выведи результат. Назови самый загруженный месяц в Markdown-ячейке.

5. Используя столбец `hire_date` из объединённого датафрейма, рассчитай стаж каждого врача в полных годах на сегодняшний день. Используй `pd.to_datetime('today')` и арифметику с объектами datetime. Добавь столбец `tenure_years`. Выведи топ-3 врача по стажу.

6. Открой файл `datasets/quarterly_report.pdf` с помощью `pdfplumber`. Извлеки таблицу из первой страницы (`.extract_table()`). Выведи «сырые» данные — ты увидишь список списков с «грязными» значениями.

7. Преобразуй извлечённые данные в датафрейм. Исправь проблемы качества данных:
   - убери лишние пробелы в строковых значениях (`.str.strip()`)
   - замени символ `'—'` на `0`
   - приведи числовые столбцы к типу `int`
   - назначь корректные имена столбцов: `['Специализация', 'Q1', 'Q2', 'Q3', 'Q4', 'Итого']`

   Выведи очищенный датафрейм.

8. Сохрани очищенный датафрейм из PDF в файл `task4_report.xlsx` с помощью `.to_excel()` без индекса (`index=False`). Сохрани файл в папку `src/`.

**Результат:** файл `task4.ipynb` + `task4_report.xlsx` в папке `src/`.

---

### Задание 5: Визуальный диагност

**Для выполнения нужно знать:** `import matplotlib.pyplot as plt`, `import seaborn as sns`, создание фигур (`fig, ax = plt.subplots()`), `plt.hist()`, `sns.barplot()`, `sns.scatterplot()`, `sns.boxplot()`, задание заголовков (`.set_title()`), подписей осей (`.set_xlabel()`, `.set_ylabel()`), цветов, легенды, `plt.savefig()`.

Данные — это ещё не знание. Врач смотрит на кардиограмму, а не на таблицу чисел. Визуализация превращает числа в образы, паттерны — в выводы. Стань визуальным диагностом клиники «Здоровье+».

> Для всех графиков используй только данные со статусом `'Проведён'`.

**Выполни следующие шаги:**

1. Постройте гистограмму распределения `wait_time_min` с помощью `matplotlib`. Укажи русский заголовок, подписи осей, задай `bins=20`. Сохрани как `chart1_wait_time.png` в папку `src/`.

2. Постройте горизонтальный столбчатый график суммарной выручки по специализациям. Специализации отсортируй по убыванию суммарной выручки. Русский заголовок и подписи осей обязательны. Сохрани как `chart2_revenue_by_specialty.png`.

3. Постройте диаграмму рассеяния: ось X — `wait_time_min`, ось Y — `revenue`, цвет точек — по специализации (`hue='specialty'`). Используй `seaborn`. Добавь легенду. Русский заголовок и подписи осей. Сохрани как `chart3_scatter.png`.

4. Постройте ящик с усами (`boxplot`): распределение `revenue` по каждой специализации. Используй `seaborn`. Русский заголовок и подписи осей. Сохрани как `chart4_boxplot.png`.

5. Все четыре графика должны:
   - иметь читаемый размер шрифта
   - не содержать визуального мусора (лишних рамок, перекрывающихся подписей, 3D-эффектов)
   - быть сохранены через `plt.savefig()`, а не скриншотами

**Результат:** файл `task5.ipynb` + 4 файла `.png` (`chart1_wait_time.png`, `chart2_revenue_by_specialty.png`, `chart3_scatter.png`, `chart4_boxplot.png`) в папке `src/`.

---

### Задание 6: Статистический детектив

**Для выполнения нужно знать:** `from scipy import stats`, функции `stats.shapiro()`, `stats.ttest_ind()`, `stats.mannwhitneyu()`, `stats.chi2_contingency()`, `pd.crosstab()`, описательная статистика (`.mean()`, `.median()`, `.mode()`, `.std()`, `.quantile()`), интерпретация p-value.

Цифры не лгут — но могут вводить в заблуждение, если не знаешь, как их читать. Статистический детектив видит не просто число, а его значимость. Три вопроса, три гипотезы — и клиника получит обоснованные рекомендации.

> Для всех расчётов используй только визиты со статусом `'Проведён'`.

**Выполни следующие шаги:**

1. Рассчитай описательные статистики для столбцов `revenue` и `wait_time_min`:
   - среднее (`.mean()`), медиана (`.median()`), мода (`.mode()`), стандартное отклонение (`.std()`)
   - 25-й, 50-й, 75-й перцентили (`.quantile([0.25, 0.5, 0.75])`)

   Выведи результаты аккуратно — можно оформить как два отдельных вывода или одну таблицу.

2. Извлеки значения `revenue` отдельно для хирургов (`'Хирургия'`) и терапевтов (`'Терапия'`). Проверь нормальность каждой выборки с помощью критерия Шапиро-Уилка (`stats.shapiro()`). Выведи p-value для каждой группы. Добавь Markdown-ячейку с выводом: можно ли считать распределение нормальным (уровень значимости α = 0.05)?

3. На основе результата шага 2 выбери подходящий критерий:
   - если **обе** выборки нормальные → `stats.ttest_ind()`
   - если хотя бы одна ненормальная → `stats.mannwhitneyu()`

   Проверь гипотезу: **«Выручка за один визит у хирургов значимо выше, чем у терапевтов»** (уровень значимости α = 0.05). Выведи статистику и p-value. Добавь Markdown-ячейку: отвергаем ли нулевую гипотезу H₀ и что это означает для клиники?

4. Проверь с помощью критерия хи-квадрат: **«Доля отменённых визитов одинакова во всех специализациях?»** Для этого:
   - построй таблицу сопряжённости (`pd.crosstab(df['specialty'], df['status'])`)
   - передай её в `stats.chi2_contingency()`
   - выведи χ²-статистику, p-value и степени свободы

   Добавь Markdown-ячейку с выводом.

5. Создай итоговую Markdown-ячейку **«Выводы и рекомендации»**. В ней:
   - кратко резюмируй результаты обоих статистических тестов
   - сформулируй 2–3 конкретных рекомендации для руководства клиники на основе полученных данных
   - укажи, какие дополнительные данные помогли бы углубить анализ

**Результат:** файл `task6.ipynb` в папке `src/`.

---

*Шесть ноутбуков. Шесть операций на живых данных. Ты научился смотреть на мир через Excel, затем через SQL. Теперь — через Python. Это не просто язык программирования: это способ думать. Загружать, исследовать, трансформировать, визуализировать, проверять гипотезы — и доверять только тому, что подтверждено данными. Такой аналитик нужен везде. Вперёд.*
